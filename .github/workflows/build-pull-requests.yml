name: Build pull requests

on:
  pull_request

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Install
      run: make install.ci
    - name: Build
      run: make build
